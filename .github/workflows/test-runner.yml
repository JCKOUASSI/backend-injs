name: TEST RUNNER

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test GitHub-hosted runner
        run: |
          echo "RUNNER OK"
          uname -a
          node --version
          python3 --version
